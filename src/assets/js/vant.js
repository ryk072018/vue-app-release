export { List, Cell, Toast, Uploader, CellGroup, Field, Button } from 'vant'

// Vue.use(Toast).use(List).use(Cell).use(Uploader).use(CellGroup).use(Field).use(Button)

// export { List, Cell, Toast, Uploader, CellGroup, Field, Button }
